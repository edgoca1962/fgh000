Para comprimir tema a partir de WP 6.4:
 zip -vr tema.zip tema/
1. Eliminar carpetas de módulos no utilizados.
2. Cambiar dominio 
   1. FGHEGC por SCC
   2. FGHEGC_ por SCC_
   3. fghegc por SCC
   4. fghegc_ por SCC
3. corecontroller: 
   1. Eliminar require getID3 y peticion_comments.
   2. Eliminar módulos no utilizados (controllers y models).
   3. get_banner function incluir is_page('beneficiario-principal') para banner = ''.
4. core->view->navbar: 
   1. Eliminar validación de login y userAdmin en el menú normal y administrador.
   2. Cambiar clase "container-fluid" por "container".
   3. incluir img logo class = "rounded-circle bg-white"
5. Modificación para Container:
   1. pagecontroller: 
      1. atributo div1 = ''
      2. incluir logica en get_datosAtributos()
      if (is_page('beneficiario-acerca')) {
         $datosAtributos['div0'] = 'background-blend';
      } else {
         $datosAtributos['div0'] = 'background-blend pt-5';
      }
   2. core->view:
      1. core-cambio-clave
      2. core-csv-files
   3. beneficiario->view: 
      1. beneficiario-mantenimiento
      2. beneficiario-csv
6. Cambiar beneficiario-principal:
   1. Incluir código de banner
   2. Cargar los atributos de Beneficiario
   3. Inlcuir codigo de logo de navbar
   4. Modificar Título y subtítulos.
7. WP-Admin:
   1. Modificar menú (en caso necesario).
   2. Modificar Home Page en Settings -> Reading.
